# Unix Command

这里只放我觉得常用的 Unix 命令。

## 目录

- [文件与目录管理](#文件与目录管理)
- [查看文件内容](#查看文件内容)
- [搜索](#搜索)
- [压缩、打包](#压缩打包)
- [进程、端口](#进程端口)
- [网络](#网络)
- [符号](#符号)
- [系统信息](#系统信息)

---

## 文件与目录管理

### ls [可选参数] [目录]

> 对应英文：List
> 关于 `ls` 的参数，多数情况下不需要使用。

列出目录内容。默认列出当前目录的文件或者目录，也可以指定对应的目录（`ls [目录]`）。

**常用参数**

- **`-l`**（long）：不止展示文件名，还会显示更多的内容：类型、权限、硬链接数、所有者、所属组、字节数、修改时间、名称。
- **`-a`**（all）：列出的内容包含隐藏文件。tips：类 Unix 系统的 `.`（当前目录）或者 `..`（上级目录）也是一个隐藏文件，所以你会看到它们俩。

### cd [目录]

> 对应英文：Change Directory

进入到对应的目录。

下面那几项其实是 **特殊路径**：

- `~`：用户目录
- `/`：根目录
- `-`：上一次所在的目录。这个还挺方便的，比如做完了某些操作后，回到之前的目录。

### pwd

> 对应英文：Print Working Directory

显示当前工作目录的绝对路径

### mkdir [目录…]

> 对应英文：Make Directory

创建目录。是的可以同时创建多个目录（`mkdir aDir bDir`），但是多件多级目录需要使用参数 `-p`。

**常用参数**

- **`-p`**（parents）：创建多级菜单（`mkdir -p a/b/c`）。

### rm [可选参数] [文件或目录…]

> 对应英文：Remove

删除指定的文件或者目录。

**常用参数**

- **`-r`**（recursive）：**非常重要**，要删除目录必加参数。
- **`-f`**（force）：强制执行，也比较重要，但是慎重使用。
- **`-v`**（verbose）：输出删除的文件或者目录名称，如果想知道删除了哪些内容，可以使用。
- **`-i`**（interactive）：交互式删除，如果不确定所有的文件都能删除时，可以通过这个命令来操作。但是需要输入 `yes` or `no` 来确认，当然也可以简写 `y` or `n`来确认。

tips：还有个 `rmdir` 的命令，它能用来删除空的目录，如果不是空的目录会报错。相比如 `rm` 会更安全一点，择情使用。

小知识点：为什么命名使用了 `rm`，而不是 `del`？因为当时小型机内存不足，命令解释器（`Shell`）为了节省内存空间，规定命令名不能超过3个字母。

### cp [可选参数] 源 目标

> 对应英文：Copy

把指定的文件或目录拷贝一份到指定的地方。

**常用参数**

- **`-r`**（recursive）：**非常重要**，要赋值目录时必加参数。
- **`-i`**（interactive）：交互式赋值，需要输入 `yes` or `no` 来确认，当然也可以简写 `y` or `n`来确认。
- **`-n`**（no clobber）：不覆盖任何已存在的目标文件，**复制目录到已存在的目录时比较常用**。
- **`-f`**（force）：不询问，强制覆盖已存在的文件。**复制目录到已存在的目录时比较常用**
- **`-u`**（update）：仅复制源文件比目标文件新更新的文件或者不存在的文件，**复制目录到已存在的目录时比较常用**

### mv [可选参数] 源 目标

> 对应英文：Move

移动文件或者目录到指定位置，如果指定了文件名，还会进行重命名。

**常用参数**

- **`-n`**（no clobber）：不覆盖任何已存在的目标文件，**复制目录到已存在的目录时比较常用**。
- **`-f`**（force）：不询问，强制覆盖已存在的文件。**复制目录到已存在的目录时比较常用**
- **`-u`**（update）：仅复制源文件比目标文件新更新的文件或者不存在的文件，**复制目录到已存在的目录时比较常用**

### touch [文件]

创建一个空文件，如果文件已存在，更新文件的更新时间。
它多数是用来更新文件的时间，如果需要创建空文件且需要编辑时，不如直接使用 `vim fileName`，不仅能创建新文件，还能直接进入到编辑界面。

---

## 查看文件内容

### cat [文件…]

> 对应英文：Catenate。哈哈哈，是的。它并不是🐱的意思，而是链条。

读取文件内容并输出到标准输出。**注意：它不适合读取大文件，因为它会一次性把内容都输出到屏幕上，会导致屏幕疯狂滚动看不清内容、占用大量的 I/O 甚至可能会卡死。**

小知识点：`cat` 的原意是链条，就说明它设计的初衷并不是打印内容，而是用来做文件拼接的。比如：`cat 第一章.txt 第二章.txt 第三章.txt > 全书.txt`。当我们执行 `cat 第一章.txt` 时它依旧在做链条，只不过只有一环，但是 Unix 又要求默认输出到屏幕，所以就能打印出内容了。

### less [文件…]

`less` 是让你可以一屏一屏的阅读内容，而不是想 `cat` 一股脑都展示出来。

和 `cat` 一样，也支持同时查看多个文件（`less 1.txt 2.txt`）。

小知识点：它的名字是因为之前有个工具叫 `more`，`less` 就是加强版的 `more`，之所以叫 `less`，是因为一句话： `less is more`（少即是多）。

**操作建**（只列最常用的）：

- `Space`（空格）：一下屏幕。记忆点：左手拇指直接敲就是下一页。
- `b`：上一屏幕。记忆点：空格的正上方就是往上一页。
- `j`：下一行。记忆点：右手的第一个手指就是下一行。
- `k`：上一行。记忆点：右手的第二个手指就是上一行。
- `/xxx`：敲回车搜索关键字。`n` 会定位到一个搜索关键字，`p` 会定位到上一个搜索关键字。如果想取消搜索，`less` 是不支持的。但是可以搜索一个不存在的内容，来达到取消搜索的效果。
- 打开多个文件时，`:n` 和 `:p` 可以切换文件。

### head / tail [可选参数] [文件]

这两个是分别查看文件的开头和文件的结尾的。会把开头和结尾的内容输出到屏幕上。都是默认打印 `10` 行。

**常用参数**

- **`-n`**（lines）：打印的行数

**使用场景**

`head` 多用于查看当前文件的内容是否是我们的目标文件。
`tail` 则多用与查看尾部增加的内容，尤其是实时增加的日志。可以使用 `-f` 进行实时的跟踪。

### wc [可选参数] [文件]

> 对应英文：word count

查看文件的数行/词/字节

**常用参数**

- **`-l`**（lines）：打印的行数

---

## 搜索

### grep [可选参数] 模式 [文件或目录]

> 对应英文：global regular expression print

`grep` 是 Unix 中最核心、最强大的**文本搜索工具**，全称：全局正则表达式打印。它的功能就是：在文件或输入流中，根据指定的“模式（Pattern）”过滤出匹配的行。它不仅仅是找关键词，**它的灵魂在于支持正则表达式**。

说明：**它用来搜索文件内容的。不能用来搜索文件**。搜索文件是 `find` 命令的工作。

tips：如果不指定文件，它会读取标准输入。

**搜索文件**

```bash
grep "xxx" xxx.txt
```

**搜索输入流**

```bash
cat xxx.txt | grep "xxx"
```

**常用参数**

- **`-r`**（recursive）：**非常重要**，搜索目录必加参数。
- **`-l`**（files with match）：**非常重要**，只列出匹配到内容的文件名。
- **`-i`**（ignore-case）：忽略大小写
- **`-w`**（word-regexp）：匹配整个单词。比如搜 `log` 时不会匹配到 `login` 或 `catalog`。
- **`-v`**（invert）：反向匹配，就是排除输入的匹配项
- **`-n`**（line number）：搜索某个具体的文件时，展示所有匹配内容的行号
- **`-c`**（count）：只展示匹配的行数，不展示匹配到的内容。支持搜索文件夹，会展示所有文件匹配到的行数，没匹配到的文件会展示 `0`。

**重要说明**

即使我们输入的是字符，但是也是按正则来进行匹配的（虽然默认的正则表达式功能没有那么全面）。所以但我们是要使用符合变成习惯的正则表达式时，就需要用到 `-E` 这个参数。但加入我们匹配一个 `IP` 地址（*127.1.1.1*），我们是不能直接写 `grep "127.1.1.1"`。因为 `.` 在正则中是**任意字符**。所以我们需要 `grep "127\.1\.1\.1"`，或者直接使用 `-F` 参数。它能让 `grep` 按固定字符串匹配。比如：`grep -F "127.1.1.1"`。

- **`-E`**（extended）：**需要了解**。更符合编程的正则语法，默认支持的正则语法没有那么全。
- **`-F`**（fixed strings）：**需要了解**。让 grep 放弃所有正则表达式的特殊含义，把搜索词当作“纯文本/普通字符串”进行字面量匹配。

那这为什么需要重要说明一下呢？因为 `grep` 还有 2 个兄弟：

- **egrep**（对应 `grep -E`）只使用正则匹配
- **fgrep**（对应 `grep -F`）只使用字符匹配

所以更推荐直接使用 `egrep` 和 `fgrep` 来做匹配。他们只考虑特定的匹配原则，所以性能也更好。

### find [搜索路径] [匹配条件] [处理动作]

说明：

- `搜索路径`：从哪里开始找（如 `.` 当前目录，`/` 根目录）。不指定路径默认是当前目录。
- `匹配条件`：定义要找什么样的文件（**匹配条件要在引号内**）。支持通配符（`*` `>` `[]` `[^]`）。
- `处理动作`：找到后干什么（默认动作是 `-print`，即打印路径）。

**常用参数**

- **`-name`**：文件名区分大小写。与之对应的就是 `-iname` 忽略文件名称大小写。
- **`-regex`**：用正则表达式匹配**完整路径**，这里很重要。是**完整路径**。比如就想匹配数字的 `txt` 文件，又不确定完整路径，可以：`find . -regex ".*/[0-9]\.txt"`。与之对应的就是 `-iregex` 忽略文件名称大小写。
- **`-type`**：按类型区分搜索
  - `-type f` 文件
  - `-type d` 目录
- **`-size`**：按大小搜索。`c` 字节，`k` KB，`M` MB，`G` GB。`+` 是大于，`-` 是小于。例如：`find . -size -1k`（小于1KB）
- **`-mtime`**：按修改时间搜索（天为单位）。`+` 是大于，`-` 是小于。例如：`find . -mtime +30`（30天前改过的）
- **`-amin`**：按访问时间搜索（分钟为单位）。`+` 是大于，`-` 是小于。例如：`find . -amin -5`（5分钟内被访问过）

**处理动作**

- **`-exec`**：对找到的文件执行某些操作，比如：`find . -name "*.sh" -exec chmod +x {} \;`。还有一个更安全的 `-ok`，每次都会询问你。
- **`-delete`**：直接删除找到的文件。**请确认清楚使用**。

### which [命令]

就是返回某个命令的绝对路径，内置的命令只会告诉你 `shell built-in command`。虽然也能判断命令是否是自带还是安装的，但这种场景最好用 `type` 命令来判断。

---

## 压缩、打包

### tar [可选参数]

> 对应英文：tape archive

`tar` 是一个打包工具，只负责打包不负责压缩。压缩一般都是指定其他工具，比如 `gzip`。

**常用参数**

- **`-v`**（verbose）：显示过程。**建议每次都加上**，这样可以看到进度。
- **`-f`**（file）：指定文件名。**建议每次都加上**，毕竟每次都要指定文件名
- **`-c`**（create）：创建压缩包。
- **`-x`**（extract）：解压缩包。
- **`-z`**：使用 `gzip` 压缩。当然也支持其他的压缩方式，多数情况就直接选择这个就好。

**使用示例**

```bash
# 在本机或 CI 环境打包
tar -zcvf dist.tar.gz dist/

# 在服务器解压到当前目录（会生成 ./dist 文件夹）
tar -zxvf dist.tar.gz

# 在服务器解压到其他目录（比如 /var/www/html）
tar -zxvf dist.tar.gz -C /var/www/html
```

当天还有 `gzip` 和 `zip` 等命令，但是我觉得没有使用场景，用的时候再查吧！

---

## 进程、端口

### lsof [可选参数]

> 对应英文：List Open Files

在 Linux 哲学里，“一切皆文件”（包括网络连接、管道、设备等）。`lsof` 就是专门来查看是谁占用了这个资源。

**常用参数**

- **`-i`**：最常用，查看谁占用了端口 `lsof -i:端口号`。其中 `PID` 是最重要的，`COMMAND` 查看程序名称。输出结果如下。

```bash
COMMAND   PID   USER   FD   TYPE  DEVICE    SIZE/OFF  NODE          NAME
node      12345 root   22u  IPv4  XXXXX     0t0       TCP *:3000    (LISTEN)
```

### kill [进程号]

找到谁占用了，要杀死这个进程：`kill PID`。
但是有一点需要清楚，`kill PID`只是通知这个进程要赶紧退出。但是，如果这时候这个进程本身就卡死了，就需要用 `kill -9 PID` 来强制杀死进程。如果可以，尽可能不用强制杀死进程。

### ps

> 对应英文：Process Status

进程快照状态，把所有正在运行的进程（程序）给你列出来。快照是静态的，要查看动态的，需要使用 `top`。
命令不需要记，多数情况都是固定的：`ps aux` 全部查看。
一般要配合 grep 来筛选出对应的进程。比如：`ps aux | grep "node"`。

---

## 网络

### ssh 用户@地址

> 对应英文：Secure Shell（安全外壳协议）

链接自己的远程服务器（`ssh root@你的服务器IP`）。如果端口被改过（比如 2222），那就是：`ssh -p 2222 root@你的服务器IP`。默认端口是 `22`.

当然每次登录都输入 IP 地址和密码都比较费事，可以在本地生成一个专门的 `ssh-key`：

```bash
# -t 指定加密算法 ed25519 首选，rsa 有点老了
# -f 指定文件名称
# -C 备注内容
ssh-keygen -t ed25519 -f ~/.ssh/my_server -C "my server"
```

然后再 `~/.ssh/config` 文件夹创建一个快捷登录的别名。内容如下：
```bash
Host myserver
  HostName 你的服务器IP
  User root
  Port 2222
  IdentityFile ~/.ssh/my_server
  IdentitiesOnly yes
```

使用 `ssh-copy-id` 把密码跟 `ssh` 进行绑定：`ssh-copy-id -i ~/.ssh/my_server.pub myserver`。这时候需要输入一次密码，以后就不需要输入密码了，直接输入：`ssh myserver` 即可。

### scp [可选参数] 源路径 目标路径

> 对应英文：Secure Copy（安全复制）

使用 SSH 协议，把本地文件拷贝到指定的服务器上，或者从服务器下载到本地。

**上传文件/目录**

上传整个目录需要 `-r`。

```bash
scp 文件 root@IP地址:目录地址
scp -r 目录 root@IP地址:目录地址
```

**下载文件/目录**

下载整个目录同样需要 `-r`。

```bash
scp root@IP地址:目录地址 文件地址
scp -r root@IP地址:目录地址 文件地址
```

如果不想要输入这么多命令，就只能配置个命令别名了，`~/.ssh/config` 就不支持了。比如在 `~/.zshrc` 中配置：`alias deploy='scp dist.tar.gz myserver:/var/www/html/'`

### ping [可选参数] 地址

> 对应英文：Packet Internet Groper（互联网包探测工具）

这个就太常用了，看看地址通不通。直接：`ping hostname`

**常用参数**

- **`-c`**（count）：`ping` 多少次。结束时会给一个统计数据。包含：丢包率、最小/平均/最大时长。
- **`-i`**（interval）：每次 `ping` 的时间间隔，默认 1 秒钟。

### curl [可选参数] URL

> 对应英文：Client URL

多数情况下就是补全一下 ping 的能力。

**常用参数**

- **`-i`**（include）：返回 Response Head
- **`-v`**（verbose）：查看更详细的各个节点握手过程

### exit

退出 ssh 连接。

### exit

退出 ssh 连接。

---

## 符号

**符号非常重要**，尤其是多个命令组合在一起的时候。

### |

管道，把左边输出变成右边输入。比如：`ps aux | grep node`。

### >（覆盖）和 >>（追加）

`>`：覆盖写入。文件里旧内容全没了，只有新内容。
`>>`：追加写入。旧内容保留，新内容接在末尾。

### 逻辑短路 && 和 ||

`&&`：**与**，前一个成功了，才执行后一个。
`||`：**或**，前一个失败了，才执行后一个。

### 快捷路径 *、~、/

`*`：通配符号。
`~`：用户目录。
`/`：根目录。

---

## 系统信息

### echo [内容]

给它什么，它就把什么打印到屏幕上。**回声** 这个名字起的挺好。**比较常用**。

### date

查看一下服务器时间。

### export 变量名称=变量内容

设置环境变量，**比较常用**。

### clear

清空屏幕，方便查看内容。
